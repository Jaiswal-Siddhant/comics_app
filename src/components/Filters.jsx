import React from 'react';
import { Text, View } from 'react-native';

const Filters = () => {
	return (
		<View style={{ width: '100%', height: 40, backgroundColor: 'wheat' }}>
			<Text>Filters Here</Text>
		</View>
	);
};

export default Filters;
